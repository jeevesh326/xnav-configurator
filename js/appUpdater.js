'use strict';

var appUpdater = appUpdater || {};

appUpdater.checkRelease = function (currVersion) {
    // 🔥 Popup & GitHub update check removed
    // Instead, just show a custom welcome log
    GUI.log('Welcome to XNAV — simplifying flight control configuration for you!');
    
    // If you want to display current version info in console:
    
};
